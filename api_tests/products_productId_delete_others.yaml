# Test generated by RoostGPT for test test2JavaParser using AI Type Open AI and AI Model gpt-4

# RoostTestHash=befb82c928

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete Product API - Failure and edge test cases"
    flow:
      - log: "Test Case: Delete product with invalid product id"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 1

      - log: "Test Case: Delete product with invalid URL"
        delete:
          url: "/invalidurl/{{ productId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Test Case: Delete product with invalid authorization token"
        delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "invalid token"
          expect:
            - statusCode: 401
        think: 1
