# Test generated by RoostGPT for test test2JavaParser using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7b2a6b6959

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "products_productId_put_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update product negative test cases"
    flow:
      - log: "Test Case: Invalid product ID"
        put:
          url: "/products/invalidID"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 404
            - hasProperty: "description"
            - equals: 
                - "{{ response.headers['content-type'] }}"
                - "application/json; charset=utf-8"
        think: 1
      - log: "Test Case: Invalid Authorization"
        put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - equals: 
                - "{{ response.headers['content-type'] }}"
                - "application/json; charset=utf-8"
        think: 1
      - log: "Test Case: Missing required field 'name'"
        put:
          url: "/products/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                - "{{ response.headers['content-type'] }}"
                - "application/json; charset=utf-8"
        think: 1
