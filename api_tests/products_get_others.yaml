# Test generated by RoostGPT for test test2JavaParser using AI Type Open AI and AI Model gpt-4

# RoostTestHash=70199667a6

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Testing failure cases, edge cases, and error handling for /products API"
    flow:
      - log: "Sending a GET request with an invalid URL"
        get:
          url: "/productss"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending a GET request without authorization"
        get:
          url: "/products"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1

      - log: "Sending a GET request with invalid authorization"
        get:
          url: "/products"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending a GET request with a non-existing product id"
        get:
          url: "/products/999999"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending a GET request with a product id of incorrect type"
        get:
          url: "/products/abc"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
