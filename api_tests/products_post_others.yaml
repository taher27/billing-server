# Test generated by RoostGPT for test test2JavaParser using AI Type Open AI and AI Model gpt-4

# RoostTestHash=dcd186097b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "products_post_others.csv"
    fields: 
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test Scenario for /products API"
    flow:
      - log: "Testing for invalid input"
        post:
          url: "/products"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "apple"
            description: ""
            price: 50
          expect:
            - statusCode: 405
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
        think: 1
      - log: "Testing for invalid auth token"
        post:
          url: "/products"
          headers:
            Authorization: "InvalidAuthToken"
            Content-Type: "application/json"
          json:
            name: "apple"
            description: "description of the product"
            price: 50
          expect:
            - statusCode: 401
        think: 1
      - log: "Testing for invalid url"
        post:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "apple"
            description: "description of the product"
            price: 50
          expect:
            - statusCode: 404
        think: 1
